# SDL2 Projects

## Compilation

Simply execute a `make all`command to compile all projects.

#### Available make targets
`make all`
`make clean`
`make clobber`
`make install`
`make uninstall`
`make help`
